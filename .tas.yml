---
framework: mocha
tier: xsmall
blocklist:
  - "test/options.spec.ts##options##cwd##"
  - "./test/options.spec.ts##options##output##should work"
postMerge:
  pattern: 
    - "./test/**/*.spec.ts"
preMerge:
  pattern:
    - "./test/**/*.spec.ts"
preRun:
     command:
        - npm ci
postRun:
     command:
        - node --version
version: 2.0
